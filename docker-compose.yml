services:
  mlops-api:
    build:
      context: .
      args: { SERVICE: mlops, PIP_INDEX_URL: "${PIP_INDEX_URL:-https://pypi.org/simple}", PIP_TRUSTED_HOST: "${PIP_TRUSTED_HOST:-}" }
    ports: ["8001:8000"]
    environment:
      MLOPS_DB_PATH: /data/mlops.db
    volumes: ["mlops-data:/data"]
    restart: unless-stopped
    read_only: true
    tmpfs: ["/tmp"]
    security_opt: ["no-new-privileges:true"]

  agent-api:
    build:
      context: .
      args: { SERVICE: agents, PIP_INDEX_URL: "${PIP_INDEX_URL:-https://pypi.org/simple}", PIP_TRUSTED_HOST: "${PIP_TRUSTED_HOST:-}" }
    ports: ["8002:8000"]
    environment:
      MODEL_PROVIDER: ${MODEL_PROVIDER:-ollama}
      MODEL_ID: ${MODEL_ID:-qwen2.5:3b}
      MODEL_BASE_URL: ${MODEL_BASE_URL:-http://ollama:11434}
      OTEL_EXPORTER_OTLP_TRACES_ENDPOINT: ${OTEL_EXPORTER_OTLP_TRACES_ENDPOINT:-}
      OPENMODELOPS_TELEMETRY_PROVIDER: ${OPENMODELOPS_TELEMETRY_PROVIDER:-otlp}
      OPENMODELOPS_CAPTURE_AI_CONTENT: ${OPENMODELOPS_CAPTURE_AI_CONTENT:-false}
    restart: unless-stopped
    read_only: true
    tmpfs: ["/tmp"]
    security_opt: ["no-new-privileges:true"]

  aiops-api:
    build:
      context: .
      args: { SERVICE: aiops, PIP_INDEX_URL: "${PIP_INDEX_URL:-https://pypi.org/simple}", PIP_TRUSTED_HOST: "${PIP_TRUSTED_HOST:-}" }
    ports: ["8003:8000"]
    restart: unless-stopped
    read_only: true
    tmpfs: ["/tmp"]
    security_opt: ["no-new-privileges:true"]

  judgeops-api:
    build:
      context: .
      args: { SERVICE: judgeops, PIP_INDEX_URL: "${PIP_INDEX_URL:-https://pypi.org/simple}", PIP_TRUSTED_HOST: "${PIP_TRUSTED_HOST:-}" }
    ports: ["8009:8000"]
    environment:
      JUDGE_PROVIDER: ${JUDGE_PROVIDER:-deterministic}
      JUDGE_MODEL_ID: ${JUDGE_MODEL_ID:-mlx-community/Qwen3-0.6B-4bit}
      JUDGE_MODEL_BASE_URL: ${JUDGE_MODEL_BASE_URL:-http://host.docker.internal:8008}
      JUDGE_MODEL_API_KEY: ${JUDGE_MODEL_API_KEY:-}
      JUDGEOPS_DATABASE: /data/judgeops.db
    volumes: ["judgeops-data:/data"]
    restart: unless-stopped
    read_only: true
    tmpfs: ["/tmp"]
    security_opt: ["no-new-privileges:true"]

  kubernetes-sre-api:
    profiles: ["kubernetes-sre"]
    build:
      context: .
      args: { SERVICE: kubernetes_sre, EXTRAS: kubernetes-sre, PIP_INDEX_URL: "${PIP_INDEX_URL:-https://pypi.org/simple}", PIP_TRUSTED_HOST: "${PIP_TRUSTED_HOST:-}" }
    ports: ["8006:8000"]
    environment:
      KUBERNETES_SRE_DB: /data/kubernetes-sre.db
      KUBERNETES_SRE_CLUSTER: ${KUBERNETES_SRE_CLUSTER:-local}
      KUBERNETES_SRE_MANAGED_NAMESPACES: ${KUBERNETES_SRE_MANAGED_NAMESPACES:-openmodelops-managed}
      KUBERNETES_SRE_EXECUTION_ENABLED: ${KUBERNETES_SRE_EXECUTION_ENABLED:-false}
      AUTH_MODE: ${AUTH_MODE:-development}
    volumes:
      - kubernetes-sre-data:/data
      - ${KUBECONFIG:-/dev/null}:/home/openmodelops/.kube/config:ro
    restart: unless-stopped
    read_only: true
    tmpfs: ["/tmp"]
    security_opt: ["no-new-privileges:true"]

  reviewer-api:
    build:
      context: .
      args: { SERVICE: reviewer, PIP_INDEX_URL: "${PIP_INDEX_URL:-https://pypi.org/simple}", PIP_TRUSTED_HOST: "${PIP_TRUSTED_HOST:-}" }
    ports: ["8005:8000"]
    environment:
      REVIEW_AGENT_CONFIG: .beacode/beacode-config.yaml
      REVIEW_AGENT_DB: /data/reviewer.db
      GITHUB_API_URL: ${GITHUB_API_URL:-https://api.github.com}
      GITHUB_TOKEN: ${GITHUB_TOKEN:-}
      AUTH_MODE: ${AUTH_MODE:-development}
    volumes: ["reviewer-data:/data"]
    restart: unless-stopped
    read_only: true
    tmpfs: ["/tmp"]
    security_opt: ["no-new-privileges:true"]

  factory-api:
    build:
      context: .
      args: { SERVICE: factory, PIP_INDEX_URL: "${PIP_INDEX_URL:-https://pypi.org/simple}", PIP_TRUSTED_HOST: "${PIP_TRUSTED_HOST:-}" }
    ports: ["8004:8000"]
    environment:
      FACTORY_DATABASE_URL: postgresql+psycopg://openmodelops:${FACTORY_DB_PASSWORD:-local-development-only}@factory-db:5432/openmodelops
      AUTH_MODE: ${AUTH_MODE:-development}
      FACTORY_ENV: ${FACTORY_ENV:-development}
      OIDC_ISSUER: ${OIDC_ISSUER:-}
      OIDC_AUDIENCE: ${OIDC_AUDIENCE:-}
      EVIDENCE_PUBLIC_KEYS_JSON: ${EVIDENCE_PUBLIC_KEYS_JSON:-{}}
      ARIA_INTEGRATION_SECRET: ${ARIA_INTEGRATION_SECRET:-}
    depends_on:
      factory-db:
        condition: service_healthy
    restart: unless-stopped
    read_only: true
    tmpfs: ["/tmp"]
    security_opt: ["no-new-privileges:true"]

  factory-db:
    image: postgres:17.6-alpine
    environment:
      POSTGRES_DB: openmodelops
      POSTGRES_USER: openmodelops
      POSTGRES_PASSWORD: ${FACTORY_DB_PASSWORD:-local-development-only}
    volumes: ["factory-db-data:/var/lib/postgresql/data"]
    configs:
      - source: factory-schema
        target: /docker-entrypoint-initdb.d/001_factory.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U openmodelops -d openmodelops"]
      interval: 5s
      timeout: 3s
      retries: 10
    restart: unless-stopped

  banking-api:
    profiles: ["banking-benchmark", "load-test"]
    build:
      context: .
      args: { SERVICE: banking, PIP_INDEX_URL: "${PIP_INDEX_URL:-https://pypi.org/simple}", PIP_TRUSTED_HOST: "${PIP_TRUSTED_HOST:-}" }
    ports: ["8007:8000"]
    restart: unless-stopped
    read_only: true
    tmpfs: ["/tmp"]
    security_opt: ["no-new-privileges:true"]
    deploy:
      resources:
        limits: {cpus: "1.0", memory: 512M}

  banking-load:
    image: grafana/k6:0.57.0
    profiles: ["load-test"]
    depends_on:
      banking-api: {condition: service_healthy}
    environment:
      BANKING_BASE_URL: http://banking-api:8000
      LOAD_PROFILE: ${LOAD_PROFILE:-smoke}
      DEBUG_ERRORS: ${DEBUG_ERRORS:-false}
    volumes:
      - ./performance/banking/k6:/scripts:ro
      - ./artifacts/performance:/results
    command: ["run", "/scripts/scenario.js"]

  ollama:
    image: ollama/ollama:0.11.4
    profiles: ["local-models"]
    ports: ["11434:11434"]
    volumes: ["ollama-data:/root/.ollama"]
    restart: unless-stopped

  qdrant:
    image: qdrant/qdrant:v1.15.1
    profiles: ["retrieval"]
    ports: ["6333:6333"]
    volumes: ["qdrant-data:/qdrant/storage"]
    restart: unless-stopped

  redis:
    image: redis:8.2-alpine
    profiles: ["feature-store"]
    command: ["redis-server", "--appendonly", "yes"]
    ports: ["6379:6379"]
    volumes: ["redis-data:/data"]
    restart: unless-stopped

  keycloak:
    image: quay.io/keycloak/keycloak:26.3.2
    profiles: ["identity"]
    command: ["start-dev", "--health-enabled=true"]
    ports: ["8180:8080"]
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KEYCLOAK_ADMIN:-admin}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:-change-me-before-shared-use}
    restart: unless-stopped

  ray-head:
    image: rayproject/ray:2.49.0-py312
    profiles: ["distributed-training"]
    command: ["ray", "start", "--head", "--dashboard-host=0.0.0.0", "--block"]
    ports: ["8265:8265", "10001:10001"]
    shm_size: 2gb
    restart: unless-stopped

  mlflow:
    image: ghcr.io/mlflow/mlflow:v3.1.4
    command: mlflow server --host 0.0.0.0 --port 5000 --backend-store-uri sqlite:////mlflow/mlflow.db --default-artifact-root /mlflow/artifacts
    ports: ["5000:5000"]
    volumes: ["mlflow-data:/mlflow"]
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:v3.5.0
    command: ["--config.file=/etc/prometheus/prometheus.yml", "--storage.tsdb.retention.time=15d"]
    ports: ["9090:9090"]
    volumes:
      - ./observability/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    restart: unless-stopped

  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.133.0
    profiles: ["telemetry"]
    command: ["--config=/etc/otelcol-contrib/config.yaml"]
    ports: ["4317:4317", "4318:4318"]
    volumes:
      - ./observability/otel-collector.yaml:/etc/otelcol-contrib/config.yaml:ro
    restart: unless-stopped

  grafana:
    image: grafana/grafana:12.1.0
    ports: ["3000:3000"]
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-change-me-before-shared-use}
    volumes:
      - grafana-data:/var/lib/grafana
      - ./observability/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./observability/grafana/dashboards:/var/lib/grafana/dashboards:ro
    restart: unless-stopped

  phoenix:
    image: arizephoenix/phoenix:version-11.32.0
    profiles: ["ai-observability"]
    ports: ["6006:6006", "4319:4317"]
    volumes: ["phoenix-data:/root/.phoenix"]
    restart: unless-stopped

volumes:
  mlops-data:
  mlflow-data:
  prometheus-data:
  grafana-data:
  ollama-data:
  qdrant-data:
  redis-data:
  factory-db-data:
  reviewer-data:
  kubernetes-sre-data:
  judgeops-data:
  phoenix-data:

configs:
  factory-schema:
    file: ./infra/postgres/001_factory.sql
