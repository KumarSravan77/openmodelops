kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - {containerPort: 30084, hostPort: 8004, protocol: TCP}
      - {containerPort: 30086, hostPort: 8006, protocol: TCP}
  - role: worker
